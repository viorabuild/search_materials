# Construction AI Agent

Инструмент для специалистов по строительству, который объединяет поиск цен на материалы, работу с Google Sheets и проверку смет в одном API и веб-интерфейсе.

## Возможности
- 🛒 Поиск цен на материалы c использованием OpenAI, веб-поиска и локальной базы данных (`data/local_materials.csv`).
- 📊 Управление Google Sheets через естественный язык: чтение, запись, генерация формул и Markdown-отчётов.
- 🧾 Проверка строительных смет с выявлением дубликатов, отсутствующих кодов и генерацией подсказок.
- 💾 Кэширование результатов для ускорения повторных запросов и экономии API-бюджета.
- 🤖 Помощник базы материалов и работ: поиск записей, подготовка вставок/обновлений/удалений и подтверждение перед применением в CSV или Google Sheets.
- 🔁 Автоматический fallback на локальную LLM (например, LM Studio `qwen/qwen3-vl-8b`) при недоступности OpenAI API.
- 🌐 Веб-интерфейс на Flask + Vue/Tailwind (`frontend/unified.html`) и CLI/программный доступ через `ConstructionAIAgent`.

## Требования
- Python 3.10 или новее.
- Ключ OpenAI (переменная `OPENAI_API_KEY`).
- (Опционально) сервисный аккаунт Google для доступа к Google Sheets.
- (Опционально) локальная LLM с OpenAI-совместимым API (например, LM Studio) для резервного режима.

## Быстрый старт
1. Клонируйте репозиторий и создайте виртуальное окружение:
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Windows: .venv\Scripts\activate
   pip install -r requirements.txt
   ```
2. Создайте конфигурацию:
   ```bash
   cp .env.example .env
   ```
   Заполните ключ OpenAI, при необходимости укажите Google Sheet ID и путь к сервисному аккаунту (`./credentials/service-account.json`).
3. Запустите веб-приложение:
   ```bash
   ./run_unified.sh
   ```
   Интерфейс будет доступен по адресу http://localhost:8501.

### Тестирование
После установки зависимостей вы можете запустить автоматические проверки:
```bash
pytest
```
Это гарантирует, что ключевые компоненты (агенты, кэш и интеграции с Google Sheets) работают корректно.

### CLI или Python API
```bash
python unified_agent.py --interactive
```
```python
from unified_agent import ConstructionAIAgent
agent = ConstructionAIAgent()
result = agent.process_command("Найди цену на цемент Portland")
print(result)
```

## Конфигурация
Ключевые переменные `.env`:
- `OPENAI_API_KEY` — обязательный ключ OpenAI.
- `GOOGLE_SERVICE_ACCOUNT_FILE` или `GOOGLE_SERVICE_ACCOUNT_JSON` — путь к файлу сервисного аккаунта.
- `GOOGLE_SHEET_ID`, `GOOGLE_SHEET_WORKSHEET` — целевая таблица и лист.
- `LOCAL_MATERIALS_CSV_PATH` — путь к локальной базе материалов (по умолчанию `./data/local_materials.csv`).
- `ENABLE_LOCAL_MATERIAL_DB` — включает загрузку локальной базы материалов (по умолчанию `true`).
- `ENABLE_MATERIALS_DB_ASSISTANT` — активирует помощника базы материалов и работ (по умолчанию `true`).
- `MATERIALS_DB_SHEET_ID`, `MATERIALS_DB_WORKSHEET` — Google-таблица и лист (или несколько листов через запятую, например `Материалы,Котировки,Работы`), где хранится база материалов.
- `MATERIALS_DB_SYNC_CSV` — синхронизировать ли изменения из Google Sheets в локальный CSV (по умолчанию `true`).
- `CACHE_DB_PATH`, `CACHE_TTL_SECONDS` — параметры кэша.
- `ENABLE_WEB_SEARCH`, `USE_BING_SEARCH`, `USE_CHATGPT_SEARCH` — включение расширенного веб-поиска.
- `ENABLE_LOCAL_LLM`, `LOCAL_LLM_BASE_URL`, `LOCAL_LLM_MODEL`, `LOCAL_LLM_API_KEY`, `LLM_REQUEST_TIMEOUT_SECONDS` — параметры резервной локальной LLM (например, LM Studio).

Если вы подключаете Google Sheets, обязательно предоставьте сервисному аккаунту права на редактирование таблицы (через «Поделиться» → email из `credentials/service-account.json`).

Полный список параметров см. в [`docs/quickstart.md`](docs/quickstart.md).

## Структура проекта
```
├── data/                     # Локальная CSV-база материалов
├── frontend/                 # Vue/Tailwind интерфейс (unified.html)
├── cache/                    # SQLite-кэш (создаётся автоматически)
├── credentials/              # Сервисные аккаунты Google (не в git)
├── docs/                     # Документация
├── advanced_agent.py         # LangChain-расширение для поиска
├── cache_manager.py          # Работа с кешем результатов
├── local_materials_db.py     # Фаззи-поиск по локальной базе
├── markdown_ai.py            # Google Sheets AI (markdown + команды)
├── material_price_agent.py   # Основной агент цен
├── site_scrapers.py          # Скрейперы поставщиков
├── gpt_scraper.py            # GPT-парсер веб-страниц
├── unified_agent.py          # Единый оркестратор
├── unified_app.py            # Flask-приложение
├── unified_examples.py       # Примеры использования
└── run_unified.sh            # Скрипт запуска
```

## Документация
- [`docs/quickstart.md`](docs/quickstart.md) — установка и запуск.
- [`docs/architecture.md`](docs/architecture.md) — архитектура системы.
- [`docs/estimate-checker.md`](docs/estimate-checker.md) — проверка смет.
- [`docs/migration.md`](docs/migration.md) — изменения после объединения проектов.
- [`docs/changelog.md`](docs/changelog.md) — история версий.
- [`docs/contributing.md`](docs/contributing.md) — вклад в проект.

## Безопасность данных
Файлы с секретами (`.env`, `credentials/*.json`, SQLite-кэш) не добавляются в git. Убедитесь, что вы не публикуете реальные ключи и сервисные аккаунты.
