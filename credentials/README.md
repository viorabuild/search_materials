Place your Google service-account JSON here as `service-account.json` (or point `GOOGLE_SERVICE_ACCOUNT_FILE` in `.env` to a different path).
This directory is intentionally empty in the repository to avoid committing secrets.
